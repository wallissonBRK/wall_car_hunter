name: Sniper de Carros 🎯

on:
  schedule:
    - cron: "0 9,15,21 * * *"
  workflow_dispatch:

jobs:
  sniper:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4

    - name: 🐍 Setup Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: 📦 Instalar Dependências (VERSÕES FIXAS)
      run: |
        pip install --upgrade pip
        pip install "supabase==2.9.1" "gotrue==2.9.0" "httpx==0.27.2"  # Fix proxy bug
        pip install requests beautifulsoup4 python-telegram-bot==21.0

    - name: 🔎 Rodar Sniper
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: python autocarro.py

    - name: ✅ Status
      run: echo "Sniper OK! ✅"
