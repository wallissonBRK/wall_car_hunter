name: Sniper de Etios 🎯

on:
  schedule:
    - cron: "0 15,21 * * *"
  workflow_dispatch:

jobs:
  buscar:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Instalar Dependências
        run: pip install requests beautifulsoup4

      - name: 🔎 Rodar Autocarro
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python autocarro.py

      - name: Commit e Push da Memória
        run: |
          git config --global user.name 'Sniper Bot'
          git config --global user.email 'bot@noreply.github.com'
          # Tenta adicionar o arquivo de memória
          git add price_memory.json
          # Só faz o commit se houver mudança
          git commit -m "Atualizando histórico de preços 📈" || exit 0
          git push
